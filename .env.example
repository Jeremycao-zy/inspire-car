# 混元 3D 凭证（可选）

# 不配置也能跑：项目会进入 DEMO 模式，上传照片后返回 public/models 下的预置模型，
# 界面、装配、ET/J/倾角实时调参全部照常可用，只是不真的消耗额度。

# 想真实调用混元 3D，把平台下发的 tempToken（tk_ 开头）或 JWT 填在这里：
# HUNYUAN3D_TOKEN=tk_xxxxxxxxxxxxxxxxxxxxxxxx

# 也可以写入文件（推荐，避免进 shell history）：
#   mkdir -p ~/.workbuddy/tokens
#   echo -n 'tk_xxx' > ~/.workbuddy/tokens/hunyuan3d

# Hyper3D (Rodin / BANG) 商业 API Key（可选）
# 配置了就走真实调用，生成与拆解会消耗 Hyper3D 额度；未配置则该引擎走 DEMO。
# HYPER3D_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxx

# 同样建议写入文件：
#   echo -n 'xxxxxxxxxxxxxxxxxxxxxxxx' > ~/.workbuddy/tokens/hyper3d

# 其他可选配置
# API_PORT=8787              # 后端端口
# MAX_POLL_MS=720000         # 单次生成最长等待（默认 12 分钟）
# POLL_INTERVAL_MS=5000      # 轮询间隔
# BUDDY_CLOUD_ENDPOINT=      # 网关地址，一般不用改
